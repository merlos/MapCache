use_frameworks!

target 'MapCache_Example' do
  pod 'MapCache', :path => '../'

  target 'MapCache_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 1.2.0'
    pod 'Nimble', '~> 7.0.2'
  end
end
